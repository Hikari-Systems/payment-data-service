# Payment data service

This service is intended to store the current payment state of a given user for a given sku (defined elsewhere) and the full list of stripe events received through the webhook
